# Pipeline Testing - Playwright Test Environment Variables
# Copy this file to .env and fill in values

# Application URLs
BASE_URL=http://localhost:8501
API_BASE_URL=http://localhost:8000

# Authentication (matches pl-test defaults for dev)
PL_TEST_USERNAME=admin
PL_TEST_PASSWORD=pltest2026

# Database (for cross-layer verification in UJTs)
DB_SERVER=localhost
DB_NAME=WiDHS.Qc.Interface.Carity.ToolTesting
DB_USERNAME=
DB_PASSWORD=

# Test Data Configuration
TEST_MCD_ID_PREFIX=000000000
TEST_INTERFACE_TYPE=icd_d06

# Mode: set to 'true' for live tests against running backend
LIVE_MODE=false
