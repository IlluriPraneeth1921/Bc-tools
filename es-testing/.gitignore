node_modules/
test-results/
reports/
playwright-report/
dist/
.env

# Auth tokens (contain secrets — never commit)
.auth/

# Reset tool user config (contains server/person preferences)
.reset-tool.json
